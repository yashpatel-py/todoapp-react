// For Firebase JS SDK v7.20.0 and later, measurementId is optional

import firebase from "firebase";

// Go to your bire base and add config code here

const db = firebaseApp.firestore();

export default db;